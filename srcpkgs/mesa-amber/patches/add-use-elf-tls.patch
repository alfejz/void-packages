diff -Naur a/meson.build b/meson.build
--- a/meson.build	2022-06-08 13:13:35.000000000 -0500
+++ b/meson.build	2023-07-13 18:01:31.638665429 -0600
@@ -510,7 +510,8 @@
 
 # Android uses emutls for versions <= P/28. For USE_ELF_TLS we need ELF TLS.
 use_elf_tls = false
-if (not ['freebsd', 'openbsd', 'haiku'].contains(host_machine.system()) and
+with_use_elf_tls = get_option('use-elf-tls')
+if (not ['freebsd', 'openbsd', 'haiku'].contains(host_machine.system()) and with_use_elf_tls and
     (not with_platform_android or get_option('platform-sdk-version') >= 29) and
     (not with_platform_windows or not with_shared_glapi))
   pre_args += '-DUSE_ELF_TLS'
diff -Naur a/meson_options.txt b/meson_options.txt
--- a/meson_options.txt	2022-06-08 13:13:35.000000000 -0500
+++ b/meson_options.txt	2023-07-13 18:04:30.974672540 -0600
@@ -489,6 +489,12 @@
   value : 25,
   description : 'Android Platform SDK version. Default: Nougat version.'
 )
+option(
+  'use-elf-tls',
+  type: 'boolean',
+  value: 'true',
+  description: 'Built support for initial exec TLS model.'
+)
 option(
   'zstd',
   type : 'combo',
